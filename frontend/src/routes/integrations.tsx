import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowUpRight, CalendarDays, Cloud, Mail, MessageCircle, ShieldCheck } from "lucide-react";
import { FAQSection } from "@/components/kurukoo/faq-section";
import { actionClass } from "@/components/kurukoo/primitives";

export const Route = createFileRoute("/integrations")({
  head: () => ({ meta: [{ title: "Integrations — Kurukoo" }, { name: "description", content: "Understand how Kurukoo connects to services, assistants and useful resources." }] }),
  component: IntegrationsPage,
});
const types = [{ title: "Storage", body: "Bring authorised files and documents into the context of a supported request.", icon: Cloud }, { title: "Messaging", body: "Use an authorised communication channel when a supported request needs someone to be contacted.", icon: MessageCircle }, { title: "Email", body: "Use relevant email context within the permissions granted to a supported connection.", icon: Mail }, { title: "Calendar", body: "Use scheduling context when a supported calendar connection is authorised.", icon: CalendarDays }];
function IntegrationsPage() { return <div className="mx-auto w-full max-w-6xl space-y-9 pb-8"><header className="max-w-3xl"><p className="text-[10.5px] font-semibold uppercase tracking-[0.14em] text-primary">Integrations</p><h1 className="mt-2 font-serif text-[40px] leading-[1.02] tracking-[-0.045em]">Bring the tools you already use into the flow.</h1><p className="mt-4 max-w-2xl text-[15px] leading-7 text-muted-foreground">Kurukoo can connect supported services and resources so authorised context can help get a job done. Connections are scoped, visible and under your control.</p><div className="mt-5 flex flex-wrap gap-2"><Link to="/signup" className={actionClass("primary")}>Get started</Link><Link to="/connect" className={actionClass()}>Open connections <ArrowUpRight className="ml-1 size-3.5" /></Link></div></header><section className="rounded-[22px] border border-border bg-surface p-5 md:p-6"><div className="flex items-start gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-brand-tint text-brand-ink"><ShieldCheck className="size-5" /></span><div><h2 className="text-[15px] font-semibold">You stay in control</h2><p className="mt-1.5 max-w-3xl text-[12.5px] leading-relaxed text-muted-foreground">A connection is a defined doorway into a service. It does not mean unlimited access or automatic permission to act. Important external actions follow the relevant approval and evidence flow.</p></div></div></section><section><p className="text-[10.5px] font-semibold uppercase tracking-[0.12em] text-muted-foreground">Connection types</p><h2 className="mt-1.5 font-serif text-[29px] tracking-[-0.035em]">Connect the context your requests need.</h2><div className="mt-4 grid gap-3 md:grid-cols-2">{types.map(({ title, body, icon: Icon }) => <article key={title} className="rounded-[19px] border border-border bg-surface p-5"><span className="grid size-9 place-items-center rounded-xl bg-elevated"><Icon className="size-4"/></span><h3 className="mt-4 text-[14.5px] font-semibold">{title}</h3><p className="mt-1.5 text-[11.5px] leading-relaxed text-muted-foreground">{body}</p></article>)}</div></section><section className="rounded-[22px] border border-border bg-elevated/35 p-5 md:p-6"><p className="text-[10.5px] font-semibold uppercase tracking-[0.12em] text-muted-foreground">External assistants</p><h2 className="mt-1.5 font-serif text-[29px] tracking-[-0.035em]">Use the conversation surface you prefer.</h2><p className="mt-2 max-w-3xl text-[12.5px] leading-relaxed text-muted-foreground">Supported integrations can make Kurukoo available through external conversation surfaces. An assistant is never treated as connected until an authorised integration actually exists.</p><div className="mt-4 grid gap-3 sm:grid-cols-2">{["ChatGPT","Claude","Gemini","Grok"].map((name) => <div key={name} className="rounded-xl border border-border bg-surface p-4"><p className="text-[13.5px] font-semibold">{name}</p><p className="mt-1 text-[11px] text-muted-foreground">Supported connection, where available and authorised.</p></div>)}</div></section><FAQSection items={[{question:"Does connecting a service give Kurukoo unlimited access?",answer:"No. A connection only exposes the scope supported and authorised by the service and account."},{question:"Can I disconnect a service?",answer:"Yes. Authenticated connection management lets you disconnect supported resources connected to your account."},{question:"Are all integrations shown here live?",answer:"No. This public page explains the integration model. Your account only shows a connection as active when the underlying service and authorisation path confirm it."}]}/></div>; }
