name: Canonical fulfilment convergence

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  canonical-fulfilment:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - run: npx tsx scripts/test-canonical-fulfilment.ts
      - run: npx tsc --noEmit
