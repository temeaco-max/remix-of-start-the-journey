{"code":"async (page) => { const routes = [{name:'checkout',path:'/cart'},{name:'confirmation',path:'/confirmation'},{name:'tasks',path:'/tasks'},{name:'connect',path:'/channels'}]; const results=[]; await page.setViewportSize({width:1280,height:720}); for (const route of routes) { const started=Date.now(); await page.goto('http://127.0.0.1:3013'+route.path,{waitUntil:'networkidle'}); await page.waitForTimeout(150); const result=await page.evaluate(() => { const visible=el=>{const r=el.getBoundingClientRect();const s=getComputedStyle(el);return r.width>0&&r.height>0&&s.visibility!=='hidden'&&s.display!=='none}; const text=el=>(el.textContent||'').trim(); const headings=Array.from(document.querySelectorAll('h1,h2,h3')).filter(visible).map(text); const missingImageAlt=Array.from(document.images).filter(img=>visible(img)&&!img.hasAttribute('alt')).length; const unnamedButtons=Array.from(document.querySelectorAll('button')).filter(btn=>visible(btn)&&!((btn.getAttribute('aria-label')||btn.getAttribute('title')||text(btn)).trim())).length; const unnamedLinks=Array.from(document.querySelectorAll('a')).filter(a=>visible(a)&&!((a.getAttribute('aria-label')||text(a)).trim())).length; const unlabeledFields=Array.from(document.querySelectorAll('input,textarea,select')).filter(field=>visible(field)&&field.type!=='hidden'&&!(field.getAttribute('aria-label')||field.getAttribute('aria-labelledby')||field.id)).length; const ids=Array.from(document.querySelectorAll('[id]')).map(e=>e.id); const duplicateIds=[...new Set(ids.filter((id,i)=>ids.indexOf(id)!==i))]; const nav=performance.getEntriesByType('navigation')[0]; const resources=performance.getEntriesByType('resource'); return {title:document.title,headings,missingImageAlt,unnamedButtons,unnamedLinks,unlabeledFields,duplicateIds,domContentLoaded:nav?.domContentLoadedEventEnd||0,loadEventEnd:nav?.loadEventEnd||0,resourceCount:resources.length,transferBytes:resources.reduce((sum,r)=>sum+(r.transferSize||0),0),visibleTextChars:text(document.body).length,scrollHeight:document.documentElement.scrollHeight}; }); result.elapsedMs=Date.now()-started; result.url=page.url(); result.name=route.name; results.push(result); } return results; }"}
