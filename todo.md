# Project TODO

Repository-side implementation and convergence ledger. External provider access is treated as a runtime dependency and is now wired into canonical activation paths; live provider/device evidence is an operational verification activity, not an unimplemented repository phase.

- [x] Canonical web/PWA/native/Admin surface convergence
- [x] Canonical Admin control plane, authentication shell and module registry
- [x] Canonical Admin dispute/escrow/Economic Request mutation path
- [x] Admin platform health, dependency readiness and scale-transition projection
- [x] Collapse legacy Admin dashboard into canonical Control Room
- [x] Canonical product truth and multi-agent activation roadmap alignment
- [x] Web secondary-surface visual convergence for pricing/contact/legal/developer surfaces
- [x] Discover, Checkout, Confirmation, Tasks, Connect and Partners convergence/regression work
- [x] Conversational identity/name plausibility guard
- [x] Capability Portfolio and bounded agent foundation
- [x] Prayer Companion repository-side capability and authentication boundaries
- [x] Guarded training/candidate curation pipeline and promotion boundary
- [x] Security cleanup for synthetic JWT fixtures
- [x] Push validated repository-side convergence milestones to main
- [x] Converged skill/execution authority propagated through runtime, outcome audit and ML training universe
- [x] Platform-wide convergence audit wired into CI
- [x] Durable DB-backed job queue with leases, retries, exponential backoff and dead-letter state
- [x] Background worker maintenance for FCM drain, provider-verification expiry, webhook-dedup retention and durable job lease recovery
- [x] Canonical provider verification lifecycle with reviewer/evidence/expiry semantics
- [x] Attachment security boundary with MIME/magic/hash checks and malware-scan handoff state
- [x] Artifact persistence gated through the attachment security boundary
- [x] Shared inbound channel webhook idempotency ledger applied to Telegram/WhatsApp
- [x] Verified/retryable outbound Telegram/WhatsApp delivery paths
- [x] External provider activation control plane for Telegram, WhatsApp, Stripe and Resend
- [x] Production boot auto-activation for configured external providers
- [x] Admin activation/probe endpoints for external provider operations
- [x] External activation contract wired into CI

## Operational verification runs

The implementation is complete for the repository/runtime boundary. The remaining work is executing live credentials/devices against the already-implemented paths and reviewing their observed evidence.

- [ ] Run the live external-provider activation probe against the deployed environment
- [ ] Confirm real device/provider delivery receipts across enabled channels
- [ ] Observe the latest main CI workflow result and keep the release line green

## Quality gates

- [x] Repository-side build/lint/route/security/PWA/accessibility/CSS/regression coverage where available
- [x] Truthful UI state semantics and fail-closed external claims
- [x] CI executes the platform-wide convergence gate on every main/PR build

## History-first visual convergence

- [x] Reconcile divergent local and origin `main` histories through a controlled non-force merge: preserve genuinely valid work, classify both divergent commit sets against canonical truth, verify repository/runtime boundaries, and push only the validated unified main. The normal push now lands on canonical `origin/main`; no force-push, rebase, or replacement integration branch was used.
- [ ] Compare the full Git history of every visual authority before modifying it, selecting the strongest complete design-and-functionality baseline per surface.
- [ ] Reconcile high-fidelity boards with the selected historical baselines across public web, Chat/workspaces, Admin, partners, checkout/confirmations, and Nearby Radar.
- [ ] Consolidate only confirmed duplicate visual overrides while preserving canonical routes, meaningful content, state truthfulness, and accessibility behavior.
- [ ] Validate the integrated surfaces at desktop and narrow widths, then record the surviving visual authority and regression evidence.
- [ ] Audit and refine current main only; do not import, rebase, or restore any PR #65/#66 or other historical branch base, and do not alter backend/platform-owned capabilities.
- [ ] Produce a machine-readable current-main inventory for every production-facing visual surface across web, PWA, native, Chat, Discover, fulfilment, agents, commerce, communications, and Admin.
- [ ] Converge shared spacing, typography, surface hierarchy, controls, semantic states, context/evidence bands, and continuation patterns using existing visual owners only.
- [ ] Prioritize Discover, Trust & Continuity, fulfilment, communication, and current operational/client surfaces while preserving verified platform truth and all canonical capability owners.
- [ ] Continue current-main presentation-only integration across the remaining public secondary, partner/fulfilment, operational/Admin, PWA and native surface owners; document exact external and physical-device validation boundaries.
- [ ] Continue the remaining current-main visual integrations in prioritized surface-owner increments, reusing existing public, Chat, workspace, commerce, agent, fulfilment, operational, PWA and native primitives only.
- [ ] Continue resolving remaining current-main visual drift by selecting and validating the next highest-impact presentation owners, with runtime and device evidence kept explicitly separate.
- [ ] Continue additional current-main visual increments across unresolved web, workspace, operational, lifecycle, and native surface owners without creating parallel components or changing platform behavior.
- [ ] Continue the next unresolved current-main surface-owner convergence cycle with shared tokens, semantic states, evidence bands, and canonical Chat continuity preserved.
- [x] Complete an implementation-level end-to-end current-main visual coverage pass across existing public, Chat, workspace, commerce, partner, operational, PWA, and native surface families; runtime, browser, provider, and device-only evidence remains explicitly tracked above.
- [ ] Inventory unfinished compatible client/presentation work across repository branches and the Manus-created workspace, classify conflicts against the current main baseline, and complete only reconciled work with exact validation evidence.
- [x] Reconcile the compatible Quick Ride branch owners: validated request/dispatch contract, registered API route implementation, mobile request-state contract, bounded offline recovery, and QR context entry; provider, payment, device and live-trip evidence remain external gates.
- [x] With current-main precedence, reconcile missing compatible branch owners from `develop` and platform-frames; preserve current canonical routes, Chat/workspace selectors, final visual authorities, and explicit external-evidence boundaries. Branch-only legacy assets and incompatible runtime work are recorded in the reconciliation audit.
- [x] Inventory and reconcile compatible unfinished visual/client work attributed to ChatGPT as well as Manus, using current-main live owners as the precedence baseline and recording non-integrable branch/runtime work separately. Identified ChatGPT-labelled commits are already contained by current main and the reconciled branch lineage; no separate ChatGPT workspace or live visual owner was found.
- [x] Continue remaining visual/client reconciliation by modifying existing current-main or branch owners only; do not add replacement documents, duplicate CSS layers, or parallel route/component files where an established owner exists.
- [x] Restore the existing truncated `src/database.ts` owner from the compatible `develop` reference, then resolve remaining current-main type-contract errors without merging broader branch files. Root TypeScript and the 56-route public contract now pass.
- [x] Inventory and reconcile compatible unfinished work from all prior agent-authored repository and branch lines, extending existing current-main owners only and recording external-evidence boundaries separately. The remaining all-domains blocker is the declared local SmolLM2 runtime timeout after 23 of 66 checks, not an unintegrated owner.
- [x] Run a controlled presentation-convergence pass across existing client owners: identify incomplete surfaces and token conflicts, then harmonize the established authorities without new UI layers or route changes. Discover continuation controls now use the canonical 44px rhythm through the existing final visual authority.
- [x] Repair the existing Partners authority after rendered local-browser inspection found inline action anchors collapsing below the 44px interaction rhythm. Primary, utility and role continuations now render through `partners-authority.css`; invitation, profile, opportunity, earnings, eligibility and provider states remain unchanged.
- [x] Repair the scoped public pricing continuation geometry through the existing final visual authority. Rendered card-level text continuations now meet the 44px interaction rhythm while the payment, escrow, provider, top-up and settlement boundaries remain explicitly pending or request-dependent.
- [x] Repair the existing Channels provider-matrix disclosure target after live inspection found the native Activation and recovery summary at 13px. The current channel-page authority now preserves native disclosure semantics at 44px without altering credentials, provider activation, recovery content or connection behavior.
- [x] Reconcile the existing native visual-contract and platform-registry owners with canonical current-main routes, Nearby Radar feedback and Admin-only capability boundaries. The focused 18-test visual/continuity/work-surface suite and 15-record platform contract now pass; two full-suite MySQL-dependent test files remain an external environment boundary.
- [x] Repair the existing Help FAQ disclosure target after rendered inspection found native common-question summaries at 15px. The shared public-page authority now preserves native disclosure semantics at 44px without changing support or external-state language.
- [x] Repair the authenticated workspace task-view tab rule after source audit found a 42px control outlier. The existing workspace authority now uses the canonical 44px interaction rhythm; signed-in Desk rendering remains separately evidence-gated.
- [x] Reconcile remaining authenticated workspace navigation targets in the same existing authority: the collapse control, sidebar navigation and More control now meet the canonical 44px interaction rhythm without changing navigation structure or state wording.
- [x] Remove the remaining narrow-workspace header icon exception: the mobile header action now remains 44px at the 560px breakpoint through the existing workspace authority.
- [x] Reconcile the existing Admin convergence navigation and narrow-width action targets to the canonical 44px rhythm without changing Admin routes, permissions, or operational state handling; authenticated Control Room validation remains access-controlled.
- [x] Complete representative current-main visual convergence coverage across rendered public, Chat, progressive-auth and Admin-entry surfaces; strict CSS, 56 public routes, public runtime, Chat/PWA and focused native contracts pass. Signed-in Desk/Admin, physical iOS/Android, external providers and the local SmolLM2 artifact remain separately evidence-gated.
- [x] Reconcile the remaining shared dynamic provider/programmatic FAQ disclosure owner: native summaries now meet the canonical 44px interaction rhythm without changing provider data, SEO content, or `details` semantics.
- [x] Reconcile the standalone PWA offline recovery owner after rendered inspection found later platform-state styles reducing manual-retry and Desk controls to 42px. Existing recovery controls now render at 44px without changing retry, offline, or send semantics.
- [x] Harmonize the standalone QR contextual-entry owner with canonical cream, terracotta, ink, border and focus tokens while preserving its existing 44px controls and explicit non-authentication, non-payment, non-activation boundaries.
- [x] Restore the intentional dark Blog hero hierarchy through the existing final authority after global ink and muted tokens suppressed its title and supporting-copy contrast; article and publication-state content remain unchanged.
- [x] Reconcile the shared public footer after the final authority resolved its surface to white while legacy typography remained dark-surface oriented. Canonical ink and muted hierarchy now applies without changing footer navigation, layout, or CTA behavior.
- [x] Reconcile API documentation in-page reference navigation through its existing public-page owner: all fragment links now render at 44px after a route-specific stylesheet cache advance, while anchors, reference content, code/schema presentation, and integration boundaries remain unchanged.
- [x] Restore the Features dark evidence-boundary heading through the existing final authority after live cascade inspection found the global ink heading token suppressing its dark-surface contrast. The component-scoped warm-cream heading preserves the current template/routing behavior, muted support copy and 64px evidence continuations.
- [x] Reconcile the existing About detail-card lifecycle continuation after live inspection found its shared text-link at 16px. The public-page owner now scopes that established link to 44px without changing its `/how-it-works` destination, support pathways, narrative content, or footer hierarchy.
- [x] Reconcile the home storefront-explanation and network-mini-card continuations after live inspection found four shared text links at 17px. The existing home owner now renders those original Chat, lifecycle, reminders, and network destinations at 44px without changing illustrative content, request state, or external boundaries.
- [x] Reconcile native rich-message Copy, Regenerate/Edit, and feedback Pressable controls after source audit found 30–32px interactive targets. The existing owner now applies 44px geometry with a focused visual-contract guard; message, feedback, editing, regeneration, and streaming behavior remain unchanged.
- [x] Reconcile the native Go Live location-consent Pressable by making its existing consent row explicitly 44px while retaining the compact checkbox mark and all location/privacy behavior. The existing visual contract now guards the target geometry; physical device touch proof remains external.
- [x] Reconcile native first-launch verification Change controls after source audit found two interactive labels without explicit target geometry. The existing owner now uses one 60px-wide, 44px-high Pressable style and a visual-contract guard without changing request, code, error, disabled, or not-provider behavior.
- [x] Reconcile the shared public navigation primitive after final screen-set measurement found visible Marketing links at 42px. The final authority now gives its intentionally quiet text-led links a 44px target while retaining white/ink Sign in and the sole terracotta Chat primary action.
- [x] Reconcile the standalone PWA recovery primary action after final screen-set comparison found its manual Retry control retained a legacy ink fill. The existing platform-state owner now uses canonical terracotta at 44px while retaining the truthful manual-retry copy, quiet Open Desk return, and focused offline composition.
- [x] Reconcile progressive-authentication consent and conversational-return continuations after live measurement found visible Terms/Privacy links at 14px and Back to chat at 21px. The existing auth owner now resolves all three at 44px without changing consent wording, identity state, email/SMS progression, or guest behavior.
- [x] Reconcile shared public-footer navigation after live measurement found fourteen Core, Support, Legal & Connect links at 11px. The final authority now keeps the existing multi-column hierarchy muted and text-led while resolving each route continuation at 44px.
- [x] Reconcile the homepage illustrative conversation send mark after a broad control scan found a 24px false interactive affordance. The existing homepage behavior owner now keeps it decorative and removes it from keyboard/assistive interaction; the real 44px How It Works continuation remains unchanged.
- [x] Reconcile the Contact support readiness continuation after live measurement found its muted channel link at 16px. The final authority now scopes only that existing secondary path to 44px while keeping Open Web Chat dominant and preserving all contact/channel-state language.
- [x] Reconcile the native feature-compass overflow action after the Kurukoo OS inventory found the existing five-tab overflow Pressable at 38px. The existing tab owner now keeps its Agent/Discover/Requests/Tasks/Connect grammar and feature-compass route while resolving only that overflow action to 44px with focused visual-contract coverage.
- [x] Reconcile canonical Chat Pin, Copy, Retry, and Delete message actions after rendered guest Chat measured the real message-mutating/clipboard/retry controls at 30px. The existing late Chat completion owner now resolves only those compact icon actions to 44×44px; browser proof preserves their `pin`, `copy`, `regenerate`, and `delete` datasets, labels, muted 15px icons, three-region Chat shell, and composer/inspector contracts.
