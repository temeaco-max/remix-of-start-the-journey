# Kurukoo roadmap

## In progress
- [x] Frontend convergence: replace duplicated inline UI with shared primitives
  (Home → Composer/Message, Work → WorkItemCard, Notifications/Explore/Memory →
  Action/IntegrationGap), remove dead UI, keep build clean.

## Later
- [ ] Replace prototype store (`src/lib/kurukoo-store.tsx`) with real Kurukoo APIs.
