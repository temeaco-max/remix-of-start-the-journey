/* Copyright (c) 2026 temeaco-max. All rights reserved. Proprietary and confidential. */
import crypto from 'node:crypto';import { getDb,saveDb } from '../database.js';import { FASTTEXT_ROUTING_CONFIG } from './fastTextRoutingConfig.js';
export type UnknownIntentDecision='accepted'|'rejected';export interface UnknownIntentSuggestion{category?:string;skill?:string;confidence?:number;provenance?:string;}
function redact(value:string){return value.replace(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/gi,'<email>').replace(/\+?\d[\d\s().-]{7,}\d/g,'<phone>').replace(/\b(?:api[_ -]?key|token|password|secret)\s*[:=]\s*[^\s,;]+/gi,'<credential>').replace(/https?:\/\/\S+/gi,'<url>').replace(/\s+/g,' ').trim().slice(0,FASTTEXT_ROUTING_CONFIG.feedback.maximumNormalizedPhraseLength);}function normalize(value:string){return redact(value).toLowerCase().replace(/[^a-z0-9<> ]/g,' ').replace(/\s+/g,' ').trim();}
export async function recordUnknownIntentCandidate(rawQuery:string,suggestion:UnknownIntentSuggestion={}):Promise<void>{const query=redact(rawQuery);const normalized=normalize(query);if(normalized.length<3)return;const db=await getDb();const existing=db.exec(`SELECT id FROM unknown_intents WHERE normalized_query=? AND status='pending' ORDER BY id DESC LIMIT 1`,[normalized])[0]?.values?.[0]?.[0] as number|undefined;if(existing)db.run(`UPDATE unknown_intents SET frequency=frequency+1, last_seen=CURRENT_TIMESTAMP, suggested_category=COALESCE(?, suggested_category), suggested_skill=COALESCE(?, suggested_skill), confidence=COALESCE(?, confidence) WHERE id=?`,[suggestion.category||null,suggestion.skill||null,suggestion.confidence??null,existing]);else db.run(`INSERT INTO unknown_intents (query, normalized_query, suggested_category, suggested_skill, confidence, provenance, status, created_at, last_seen) VALUES (?, ?, ?, ?, ?, ?, 'pending', CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)`,[query,normalized,suggestion.category||null,suggestion.skill||null,suggestion.confidence??null,suggestion.provenance||'classifier_abstention']);saveDb();}
export async function listUnknownIntentFeedback(limit=50){const db=await getDb();const safeLimit=Math.max(1,Math.min(200,Math.floor(limit)));const rows=db.exec(`SELECT id, query, normalized_query, suggested_category, suggested_skill, confidence, status, frequency, provenance, reviewer, reviewed_at, training_lineage_id, created_at, last_seen FROM unknown_intents ORDER BY CASE status WHEN 'pending' THEN 0 ELSE 1 END, frequency DESC, last_seen DESC LIMIT ?`,[safeLimit])[0]?.values||[];const columns=['id','query','normalized_query','suggested_category','suggested_skill','confidence','status','frequency','provenance','reviewer','reviewed_at','training_lineage_id','created_at','last_seen'];return rows.map((values:unknown[])=>Object.fromEntries(columns.map((column,index)=>[column,values[index]??null])));}
export async function getUnknownIntentFeedbackSummary(){const db=await getDb();const count=(sql:string)=>Number(db.exec(sql)[0]?.values?.[0]?.[0]||0);const pending=count(`SELECT COUNT(*) FROM unknown_intents WHERE status='pending'`),total=count(`SELECT COUNT(*) FROM unknown_intents`),accepted=count(`SELECT COUNT(*) FROM unknown_intents WHERE status='accepted'`);const unresolvedCategories=db.exec(`SELECT COALESCE(suggested_category,'unclassified') AS category,SUM(frequency) AS volume FROM unknown_intents WHERE status='pending' GROUP BY category ORDER BY volume DESC LIMIT 12`)[0]?.values||[];const topPhrases=db.exec(`SELECT query,suggested_category,suggested_skill,SUM(frequency) AS volume FROM unknown_intents WHERE status='pending' GROUP BY normalized_query ORDER BY volume DESC,last_seen DESC LIMIT 20`)[0]?.values||[];return{total,pending,accepted,acceptanceRate:total?accepted/total:null,unresolvedCategories:unresolvedCategories.map((row:unknown[])=>({category:row[0],volume:Number(row[1])})),topUnresolvedPhrases:topPhrases.map((row:unknown[])=>({query:row[0],category:row[1],skill:row[2],volume:Number(row[3])})),retentionDays:FASTTEXT_ROUTING_CONFIG.feedback.retentionDays,trainingPolicy:'reviewed_candidates_only_no_raw_traffic_auto_training'};}
export async function reviewUnknownIntentCandidate(id:number,decision:UnknownIntentDecision,reviewer:string,acceptedExample?:{label:string;text:string}){if(!Number.isInteger(id)||id<1)throw new Error('Invalid unknown-intent candidate ID');if(!['accepted','rejected'].includes(decision))throw new Error('Invalid review decision');const db=await getDb();const exists=db.exec(`SELECT id FROM unknown_intents WHERE id=?`,[id])[0]?.values?.[0]?.[0];if(!exists)throw new Error('Unknown-intent candidate not found');const lineage=decision==='accepted'&&acceptedExample?`fasttext-review:${crypto.randomUUID()}`:null;db.run(`UPDATE unknown_intents SET status=?, reviewer=?, reviewed_at=CURRENT_TIMESTAMP, training_lineage_id=COALESCE(?, training_lineage_id) WHERE id=?`,[decision,reviewer.slice(0,120),lineage,id]);db.run(`INSERT INTO audit_logs (action, details) VALUES (?, ?)`,['fasttext_unknown_intent_review',JSON.stringify({id,decision,reviewer:reviewer.slice(0,120),lineage,acceptedExample:acceptedExample?{label:acceptedExample.label.slice(0,120),text:redact(acceptedExample.text)}:null,automaticTraining:false})]);saveDb();return{id,decision,trainingLineageId:lineage,automaticTraining:false};}
export async function purgeExpiredUnknownIntentFeedback(now=Date.now()){const db=await getDb();const cutoff=new Date(now-FASTTEXT_ROUTING_CONFIG.feedback.retentionDays*86_400_000).toISOString();db.run(`DELETE FROM unknown_intents WHERE status IN ('rejected','accepted') AND COALESCE(reviewed_at,last_seen,created_at) < ?`,[cutoff]);const removed=Number(db.getRowsModified?.()||0);if(removed)saveDb();return removed;}
