import { getCanonicalStore } from './canonicalStore.js';
import { getCanonicalPersistenceMode } from './canonicalPersistence.js';
async function ensure(sqljsSql:string,postgresSql:string){const s=await getCanonicalStore();await s.run(getCanonicalPersistenceMode()==='postgres'?postgresSql:sqljsSql);return s;}
export async function ensureOrderSchema(){return ensure(`CREATE TABLE IF NOT EXISTS orders(id TEXT PRIMARY KEY,phone TEXT,order_type TEXT,provider_phone TEXT,amount INTEGER,status TEXT,idempotency_key TEXT UNIQUE,created_at TEXT DEFAULT CURRENT_TIMESTAMP,message TEXT,updated_at TEXT DEFAULT CURRENT_TIMESTAMP)`,`CREATE TABLE IF NOT EXISTS orders(id TEXT PRIMARY KEY,phone TEXT,order_type TEXT,provider_phone TEXT,amount BIGINT,status TEXT,idempotency_key TEXT UNIQUE,created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,message TEXT,updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`);}
export async function ensureSavedSchema(){return ensure(`CREATE TABLE IF NOT EXISTS saved_items(id TEXT PRIMARY KEY,phone TEXT NOT NULL,kind TEXT NOT NULL DEFAULT 'context',title TEXT NOT NULL,description TEXT,source_url TEXT,source_id TEXT,metadata TEXT DEFAULT '{}',created_at TEXT DEFAULT CURRENT_TIMESTAMP,updated_at TEXT DEFAULT CURRENT_TIMESTAMP,UNIQUE(phone,kind,source_id))`,`CREATE TABLE IF NOT EXISTS saved_items(id TEXT PRIMARY KEY,phone TEXT NOT NULL,kind TEXT NOT NULL DEFAULT 'context',title TEXT NOT NULL,description TEXT,source_url TEXT,source_id TEXT,metadata TEXT DEFAULT '{}',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,UNIQUE(phone,kind,source_id))`);}
export async function ensureCartSchema(){return ensure(`CREATE TABLE IF NOT EXISTS cart_items(id TEXT PRIMARY KEY,phone TEXT NOT NULL,offer_id TEXT NOT NULL,title TEXT NOT NULL,seller TEXT,quantity INTEGER NOT NULL DEFAULT 1,price_minor INTEGER,currency TEXT DEFAULT 'NGN',source TEXT,provenance TEXT,external_url TEXT,media_reference TEXT,request_id TEXT,status TEXT DEFAULT 'review',created_at TEXT DEFAULT CURRENT_TIMESTAMP,UNIQUE(phone,offer_id));CREATE TABLE IF NOT EXISTS affiliate_clicks(id INTEGER PRIMARY KEY AUTOINCREMENT,product TEXT)`,`CREATE TABLE IF NOT EXISTS cart_items(id TEXT PRIMARY KEY,phone TEXT NOT NULL,offer_id TEXT NOT NULL,title TEXT NOT NULL,seller TEXT,quantity INTEGER NOT NULL DEFAULT 1,price_minor BIGINT,currency TEXT DEFAULT 'NGN',source TEXT,provenance TEXT,external_url TEXT,media_reference TEXT,request_id TEXT,status TEXT DEFAULT 'review',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,UNIQUE(phone,offer_id));CREATE TABLE IF NOT EXISTS affiliate_clicks(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,product TEXT)`);}
export async function ensureTrustSchema(){const s=await ensure(`CREATE TABLE IF NOT EXISTS trust_score_ledger(id INTEGER PRIMARY KEY AUTOINCREMENT,phone TEXT NOT NULL,score REAL NOT NULL,breakdown_json TEXT NOT NULL,reason TEXT NOT NULL,created_at TEXT DEFAULT CURRENT_TIMESTAMP)`,`CREATE TABLE IF NOT EXISTS trust_score_ledger(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,phone TEXT NOT NULL,score DOUBLE PRECISION NOT NULL,breakdown_json TEXT NOT NULL,reason TEXT NOT NULL,created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`);await s.run('CREATE INDEX IF NOT EXISTS idx_trust_score_ledger_phone ON trust_score_ledger(phone,created_at DESC)');return s;}
export async function ensurePointsSchema(){return ensure(`CREATE TABLE IF NOT EXISTS credit_transactions(id INTEGER PRIMARY KEY AUTOINCREMENT,phone TEXT,amount INTEGER,type TEXT,description TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP)`,`CREATE TABLE IF NOT EXISTS credit_transactions(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,phone TEXT,amount INTEGER,type TEXT,description TEXT,created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`);}
export async function ensureEscrowSchemaCanonical(){return ensure(`CREATE TABLE IF NOT EXISTS escrow(id INTEGER PRIMARY KEY AUTOINCREMENT,order_id TEXT,buyer_phone TEXT,provider_phone TEXT,amount_minor INTEGER,description TEXT,status TEXT DEFAULT 'held',created_at TEXT DEFAULT CURRENT_TIMESTAMP,booking_type TEXT,cooling_off_until TEXT,completed_at TEXT,dispute_reason TEXT,beneficiary_phone TEXT,provider_identity TEXT,delegated_provider INTEGER DEFAULT 0)`,`CREATE TABLE IF NOT EXISTS escrow(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,order_id TEXT,buyer_phone TEXT,provider_phone TEXT,amount_minor BIGINT,description TEXT,status TEXT DEFAULT 'held',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,booking_type TEXT,cooling_off_until TIMESTAMPTZ,completed_at TIMESTAMPTZ,dispute_reason TEXT,beneficiary_phone TEXT,provider_identity TEXT,delegated_provider BOOLEAN DEFAULT FALSE)`);}
export async function ensureCommercialSchemaCanonical(){const s=await ensure(`CREATE TABLE IF NOT EXISTS commercial_ledger(id INTEGER PRIMARY KEY AUTOINCREMENT,event_type TEXT NOT NULL,direction TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',currency TEXT NOT NULL,gross_minor INTEGER NOT NULL DEFAULT 0,platform_fee_minor INTEGER NOT NULL DEFAULT 0,provider_amount_minor INTEGER NOT NULL DEFAULT 0,processing_fee_minor INTEGER NOT NULL DEFAULT 0,tax_minor INTEGER NOT NULL DEFAULT 0,net_minor INTEGER NOT NULL DEFAULT 0,payer TEXT,payee TEXT,represented_party TEXT,agent_id TEXT,skill TEXT,economic_request_id TEXT,external_reference TEXT,idempotency_key TEXT NOT NULL UNIQUE,metadata TEXT NOT NULL DEFAULT '{}',created_at TEXT DEFAULT CURRENT_TIMESTAMP,settled_at TEXT)`,`CREATE TABLE IF NOT EXISTS commercial_ledger(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,event_type TEXT NOT NULL,direction TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',currency TEXT NOT NULL,gross_minor BIGINT NOT NULL DEFAULT 0,platform_fee_minor BIGINT NOT NULL DEFAULT 0,provider_amount_minor BIGINT NOT NULL DEFAULT 0,processing_fee_minor BIGINT NOT NULL DEFAULT 0,tax_minor BIGINT NOT NULL DEFAULT 0,net_minor BIGINT NOT NULL DEFAULT 0,payer TEXT,payee TEXT,represented_party TEXT,agent_id TEXT,skill TEXT,economic_request_id TEXT,external_reference TEXT,idempotency_key TEXT NOT NULL UNIQUE,metadata TEXT NOT NULL DEFAULT '{}',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,settled_at TIMESTAMPTZ)`);await s.run('CREATE INDEX IF NOT EXISTS idx_commercial_ledger_request ON commercial_ledger(economic_request_id,created_at DESC)');await s.run('CREATE INDEX IF NOT EXISTS idx_commercial_ledger_status_created ON commercial_ledger(status,created_at DESC)');await s.run(getCanonicalPersistenceMode()==='postgres'?`CREATE TABLE IF NOT EXISTS commercial_fee_rules(code TEXT PRIMARY KEY,mode TEXT NOT NULL,amount_minor BIGINT NOT NULL DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',active BOOLEAN NOT NULL DEFAULT FALSE,description TEXT NOT NULL DEFAULT '',updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`:`CREATE TABLE IF NOT EXISTS commercial_fee_rules(code TEXT PRIMARY KEY,mode TEXT NOT NULL,amount_minor INTEGER NOT NULL DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',active INTEGER NOT NULL DEFAULT 0,description TEXT NOT NULL DEFAULT '',updated_at TEXT DEFAULT CURRENT_TIMESTAMP)`);await s.run(getCanonicalPersistenceMode()==='postgres'?`CREATE TABLE IF NOT EXISTS agent_delegations(id TEXT PRIMARY KEY,owner_phone TEXT NOT NULL,agent_id TEXT NOT NULL,skill TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'active',instructions TEXT NOT NULL DEFAULT '',authority_json TEXT NOT NULL DEFAULT '{}',commercial_product TEXT NOT NULL DEFAULT 'agent_service',monthly_price_minor BIGINT NOT NULL DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`:`CREATE TABLE IF NOT EXISTS agent_delegations(id TEXT PRIMARY KEY,owner_phone TEXT NOT NULL,agent_id TEXT NOT NULL,skill TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'active',instructions TEXT NOT NULL DEFAULT '',authority_json TEXT NOT NULL DEFAULT '{}',commercial_product TEXT NOT NULL DEFAULT 'agent_service',monthly_price_minor INTEGER NOT NULL DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',created_at TEXT DEFAULT CURRENT_TIMESTAMP,updated_at TEXT DEFAULT CURRENT_TIMESTAMP)`);await s.run(`CREATE TABLE IF NOT EXISTS agent_permissions(delegation_id TEXT PRIMARY KEY,allow_donations ${getCanonicalPersistenceMode()==='postgres'?'BOOLEAN':'INTEGER'} DEFAULT ${getCanonicalPersistenceMode()==='postgres'?'FALSE':'0'},donation_recipient TEXT)`);await s.run(getCanonicalPersistenceMode()==='postgres'?`CREATE TABLE IF NOT EXISTS commercial_products(code TEXT PRIMARY KEY,name TEXT NOT NULL,product_type TEXT NOT NULL,price_minor BIGINT NOT NULL DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',billing_period TEXT NOT NULL DEFAULT 'one_time',active BOOLEAN NOT NULL DEFAULT TRUE,metadata TEXT NOT NULL DEFAULT '{}',updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`:`CREATE TABLE IF NOT EXISTS commercial_products(code TEXT PRIMARY KEY,name TEXT NOT NULL,product_type TEXT NOT NULL,price_minor INTEGER NOT NULL DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',billing_period TEXT NOT NULL DEFAULT 'one_time',active INTEGER NOT NULL DEFAULT 1,metadata TEXT NOT NULL DEFAULT '{}',updated_at TEXT DEFAULT CURRENT_TIMESTAMP)`);await s.run(getCanonicalPersistenceMode()==='postgres'?`CREATE TABLE IF NOT EXISTS affiliate_conversions(id TEXT PRIMARY KEY,partner TEXT NOT NULL,click_id TEXT,external_order_id TEXT,gross_minor BIGINT DEFAULT 0,commission_minor BIGINT DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',status TEXT NOT NULL DEFAULT 'pending',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,settled_at TIMESTAMPTZ)`:`CREATE TABLE IF NOT EXISTS affiliate_conversions(id TEXT PRIMARY KEY,partner TEXT NOT NULL,click_id TEXT,external_order_id TEXT,gross_minor INTEGER DEFAULT 0,commission_minor INTEGER DEFAULT 0,currency TEXT NOT NULL DEFAULT 'NGN',status TEXT NOT NULL DEFAULT 'pending',created_at TEXT DEFAULT CURRENT_TIMESTAMP,settled_at TEXT)`);return s;}
export async function ensurePaymentWebhookSchema(){return ensure(`CREATE TABLE IF NOT EXISTS payment_webhook_events(provider TEXT NOT NULL,event_id TEXT NOT NULL,received_at TEXT DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(provider,event_id))`,`CREATE TABLE IF NOT EXISTS payment_webhook_events(provider TEXT NOT NULL,event_id TEXT NOT NULL,received_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,PRIMARY KEY(provider,event_id))`);}
export async function ensurePulseSchema(){return ensure(`CREATE TABLE IF NOT EXISTS pulse_sessions(id INTEGER PRIMARY KEY AUTOINCREMENT,phone TEXT,skill TEXT,lat REAL,lng REAL,expires_at TEXT,active INTEGER DEFAULT 1)`,`CREATE TABLE IF NOT EXISTS pulse_sessions(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,phone TEXT,skill TEXT,lat DOUBLE PRECISION,lng DOUBLE PRECISION,expires_at TIMESTAMPTZ,active BOOLEAN DEFAULT TRUE)`);}
export async function ensureDisputeSchema(){return ensure(`CREATE TABLE IF NOT EXISTS disputes(id INTEGER PRIMARY KEY AUTOINCREMENT,phone TEXT,order_id TEXT,reason TEXT,status TEXT DEFAULT 'open',resolution TEXT,type TEXT DEFAULT 'dispute',fault_party TEXT,fault_phone TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP)`,`CREATE TABLE IF NOT EXISTS disputes(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,phone TEXT,order_id TEXT,reason TEXT,status TEXT DEFAULT 'open',resolution TEXT,type TEXT DEFAULT 'dispute',fault_party TEXT,fault_phone TEXT,created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP)`);}
export async function ensurePrivacyBridgeSchema(){return ensure(`CREATE TABLE IF NOT EXISTS privacy_bridge(id INTEGER PRIMARY KEY AUTOINCREMENT,real_phone TEXT NOT NULL,proxy_phone TEXT NOT NULL,context TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'active',created_at TEXT DEFAULT CURRENT_TIMESTAMP,expires_at TEXT,released_at TEXT,UNIQUE(real_phone,proxy_phone))`,`CREATE TABLE IF NOT EXISTS privacy_bridge(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,real_phone TEXT NOT NULL,proxy_phone TEXT NOT NULL,context TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'active',created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,expires_at TIMESTAMPTZ,released_at TIMESTAMPTZ,UNIQUE(real_phone,proxy_phone))`);}
export async function ensureFcmDeviceSchema(){return ensure(`CREATE TABLE IF NOT EXISTS fcm_devices(id INTEGER PRIMARY KEY AUTOINCREMENT,phone TEXT NOT NULL,device_id TEXT NOT NULL,token TEXT NOT NULL,platform TEXT NOT NULL DEFAULT 'unknown',credential_type TEXT NOT NULL DEFAULT 'fcm',label TEXT,active INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,last_accepted_at TEXT,last_error TEXT,UNIQUE(phone,device_id))`,`CREATE TABLE IF NOT EXISTS fcm_devices(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,phone TEXT NOT NULL,device_id TEXT NOT NULL,token TEXT NOT NULL,platform TEXT NOT NULL DEFAULT 'unknown',credential_type TEXT NOT NULL DEFAULT 'fcm',label TEXT,active BOOLEAN NOT NULL DEFAULT TRUE,created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,last_accepted_at TIMESTAMPTZ,last_error TEXT,UNIQUE(phone,device_id))`);}
export async function ensureProgressiveTrustSchema(){const s=await ensure(`CREATE TABLE IF NOT EXISTS trusted_devices(id INTEGER PRIMARY KEY AUTOINCREMENT,phone TEXT NOT NULL,device_hash TEXT NOT NULL,credential_type TEXT NOT NULL DEFAULT 'web',label TEXT,status TEXT NOT NULL DEFAULT 'active',push_capable INTEGER NOT NULL DEFAULT 0,created_at TEXT DEFAULT CURRENT_TIMESTAMP,last_seen_at TEXT DEFAULT CURRENT_TIMESTAMP,revoked_at TEXT,UNIQUE(phone,device_hash));CREATE TABLE IF NOT EXISTS trust_challenges(id TEXT PRIMARY KEY,phone TEXT NOT NULL,target_device_hash TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',purpose TEXT NOT NULL DEFAULT 'device_sign_in',expires_at TEXT NOT NULL,approved_by_device_hash TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP,approved_at TEXT);CREATE TABLE IF NOT EXISTS channel_evidence(id INTEGER PRIMARY KEY AUTOINCREMENT,phone TEXT NOT NULL,channel TEXT NOT NULL,evidence_type TEXT NOT NULL,external_subject TEXT,status TEXT NOT NULL DEFAULT 'observed',source_ref TEXT,consented INTEGER NOT NULL DEFAULT 0,observed_at TEXT DEFAULT CURRENT_TIMESTAMP,revoked_at TEXT,UNIQUE(phone,channel,evidence_type,external_subject));CREATE TABLE IF NOT EXISTS location_consents(id INTEGER PRIMARY KEY AUTOINCREMENT,phone TEXT NOT NULL,purpose TEXT NOT NULL,precision TEXT NOT NULL DEFAULT 'coarse',latitude REAL,longitude REAL,area TEXT,status TEXT NOT NULL DEFAULT 'granted',expires_at TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP,revoked_at TEXT);`,`CREATE TABLE IF NOT EXISTS trusted_devices(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,phone TEXT NOT NULL,device_hash TEXT NOT NULL,credential_type TEXT NOT NULL DEFAULT 'web',label TEXT,status TEXT NOT NULL DEFAULT 'active',push_capable BOOLEAN NOT NULL DEFAULT FALSE,created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,last_seen_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,revoked_at TIMESTAMPTZ,UNIQUE(phone,device_hash));CREATE TABLE IF NOT EXISTS trust_challenges(id TEXT PRIMARY KEY,phone TEXT NOT NULL,target_device_hash TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',purpose TEXT NOT NULL DEFAULT 'device_sign_in',expires_at TIMESTAMPTZ NOT NULL,approved_by_device_hash TEXT,created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,approved_at TIMESTAMPTZ);CREATE TABLE IF NOT EXISTS channel_evidence(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,phone TEXT NOT NULL,channel TEXT NOT NULL,evidence_type TEXT NOT NULL,external_subject TEXT,status TEXT NOT NULL DEFAULT 'observed',source_ref TEXT,consented BOOLEAN NOT NULL DEFAULT FALSE,observed_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,revoked_at TIMESTAMPTZ);CREATE TABLE IF NOT EXISTS location_consents(id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,phone TEXT NOT NULL,purpose TEXT NOT NULL,precision TEXT NOT NULL DEFAULT 'coarse',latitude DOUBLE PRECISION,longitude DOUBLE PRECISION,area TEXT,status TEXT NOT NULL DEFAULT 'granted',expires_at TIMESTAMPTZ,created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,revoked_at TIMESTAMPTZ);`);return s;}
export async function ensureAiQuotaSchema(){return ensure(`CREATE TABLE IF NOT EXISTS ai_usage_daily(phone TEXT NOT NULL,day TEXT NOT NULL,simple_requests INTEGER DEFAULT 0,complex_requests INTEGER DEFAULT 0,tokens_est INTEGER DEFAULT 0,PRIMARY KEY(phone,day));CREATE TABLE IF NOT EXISTS ai_usage_minute(bucket TEXT PRIMARY KEY,complex_count INTEGER DEFAULT 0)`,`CREATE TABLE IF NOT EXISTS ai_usage_daily(phone TEXT NOT NULL,day TEXT NOT NULL,simple_requests INTEGER DEFAULT 0,complex_requests INTEGER DEFAULT 0,tokens_est INTEGER DEFAULT 0,PRIMARY KEY(phone,day));CREATE TABLE IF NOT EXISTS ai_usage_minute(bucket TEXT PRIMARY KEY,complex_count INTEGER DEFAULT 0)`);}
