/* Copyright (c) 2026 temeaco-max. All rights reserved. Proprietary and confidential. */
export interface LruEntry<T>{value:T;expiresAt:number;}
/** Map-backed LRU: reads refresh recency and expired entries are never returned. */
export class LruCache<T>{private readonly entries=new Map<string,LruEntry<T>>();constructor(private readonly maxEntries:number){}get(key:string,now=Date.now()):T|undefined{const entry=this.entries.get(key);if(!entry)return undefined;if(entry.expiresAt<=now){this.entries.delete(key);return undefined;}this.entries.delete(key);this.entries.set(key,entry);return entry.value;}set(key:string,value:T,ttlMs:number,now=Date.now()):void{this.entries.delete(key);this.entries.set(key,{value,expiresAt:now+ttlMs});while(this.entries.size>this.maxEntries)this.entries.delete(this.entries.keys().next().value as string);}get size(){return this.entries.size;}clear(){this.entries.clear();}}
