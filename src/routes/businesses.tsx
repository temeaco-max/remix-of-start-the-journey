import { createFileRoute, Link } from "@tanstack/react-router";
import { useState } from "react";
import { PageHeader } from "@/components/app-shell";
import { EntityCard } from "@/components/kurukoo/cards";
import { Action, actionClass } from "@/components/kurukoo/primitives";
import { Rows, SectionHeader, StatTile, Tabs } from "@/components/kurukoo/ui";
import { entities } from "@/lib/kurukoo-demo";

export const Route = createFileRoute("/businesses")({ head: () => ({ meta: [
  { title: "Businesses — Kurukoo" },
  { name: "description", content: "Discover businesses, or run yours on Kurukoo with requests, customers, advertising and analytics." },
] }), component: BusinessesPage });
const tabs=["Discover","Your business"] as const;
function BusinessesPage(){const [tab,setTab]=useState<string>(tabs[0]);const list=entities.filter((e)=>e.kind==="business");return <><PageHeader title="Businesses" subtitle="Services, products and teams people can reach through Kurukoo."/><Tabs items={tabs} value={tab} onChange={setTab}/>{tab==="Discover"?<div className="mt-4 grid gap-3">{list.map((b)=><EntityCard key={b.id} entity={b}/>)}</div>:<div className="mt-4 space-y-6"><section className="rounded-2xl border border-border bg-elevated/50 p-4"><p className="text-[15px] font-semibold">Your business on Kurukoo</p><p className="mt-1.5 text-[12.5px] leading-relaxed text-muted-foreground">Make it easier for people to find what you offer, respond to requests and understand demand.</p><div className="mt-3 flex flex-wrap gap-2"><Action variant="primary">Set up business</Action><Link to="/pricing" className={actionClass()}>See business plan</Link><Link to="/advertising" className={actionClass()}>Advertising</Link></div></section><div className="grid gap-3 sm:grid-cols-3"><StatTile label="Incoming requests" value="0" note="Ready when connected"/><StatTile label="Customers" value="0" note="Customer history"/><StatTile label="Ad spend" value="£0.00" note="Measured when connected"/></div><section><SectionHeader title="Business tools"/><Rows>{[["Profile and location","How people find you","/businesses"],["Services and products","What you offer","/capabilities"],["Team","Who can respond","/network"],["Requests and work","Jobs in progress","/work"],["Messages","Customer conversations","/messages"],["Analytics","Demand and performance","/activity"],["Advertising","Sponsored placements","/advertising"],["Subscriptions and payments","Plan and billing","/subscriptions"],["Verification","Trust and safety","/settings"]].map(([title,note,to])=><li key={title} className="flex items-center justify-between gap-4 px-4 py-3.5"><span className="min-w-0"><span className="block text-[15px]">{title}</span><span className="block text-[13px] text-muted-foreground">{note}</span></span><Link to={to as never} className="shrink-0 text-[12px] font-medium underline">Open</Link></li>)}</Rows><div className="mt-3 flex flex-wrap gap-2"><Link to="/advertising" className={actionClass("primary")}>Advertising</Link><Link to="/subscriptions" className={actionClass()}>Subscription</Link><Link to="/wallet" className={actionClass()}>Wallet</Link></div></section></div>}</>}
