import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowLeft, Bot, CheckCircle2, LockKeyhole, Play, ShieldCheck } from "lucide-react";
import { useEffect, useState } from "react";
import { kurukooAIProviders } from "@/components/kurukoo/ai-provider-directory";
import { Action, actionClass } from "@/components/kurukoo/primitives";
import { CapabilityPills, DirectoryStateBadge } from "@/components/kurukoo/surface-directory";
import { Panel } from "@/components/kurukoo/ui";

export const Route = createFileRoute("/agents/$agentId")({ head: () => ({ meta: [{ title: "Agent — Kurukoo" }] }), component: AgentDetailPage });
function AgentDetailPage() { const { agentId } = Route.useParams(); const agent = kurukooAIProviders.find((item) => item.id === agentId); const [started, setStarted] = useState(false); useEffect(() => { if (agent) document.title = `${agent.name} — Kurukoo`; }, [agent]); if (!agent) return <div className="mx-auto max-w-3xl py-12"><Link to="/agents" className="text-[11px] font-medium">Back to Agents</Link><h1 className="mt-5 font-serif text-3xl">Agent not found</h1><p className="mt-2 text-sm text-muted-foreground">This agent is not in the current Kurukoo catalogue.</p></div>; const start = () => { localStorage.setItem("kurukoo-chat-draft", `${agent.prompt}\n\nStart with: ${agent.jobs[0] ?? "Help me with my request"}.`); setStarted(true); }; return <div className="mx-auto w-full max-w-5xl space-y-7 pb-10"><Link to="/agents" className="inline-flex items-center gap-1.5 text-[10.5px] font-medium text-muted-foreground hover:text-foreground"><ArrowLeft className="size-3.5"/> All agents</Link><header className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div className="flex items-start gap-3"><span className="grid size-12 shrink-0 place-items-center rounded-2xl bg-brand-tint text-brand-ink"><Bot className="size-5"/></span><div><div className="flex flex-wrap items-center gap-2"><h1 className="font-serif text-[34px] leading-none tracking-[-0.04em]">{agent.name}</h1><DirectoryStateBadge state="example"/></div><p className="mt-2 text-[11.5px] text-muted-foreground">{agent.role} · {agent.category}</p><p className="mt-2 max-w-2xl text-[13px] leading-6 text-muted-foreground">{agent.description}</p></div></div><div className="flex flex-wrap gap-2"><button type="button" onClick={start} className={actionClass("primary")}><Play className="mr-1.5 size-3.5"/>Use this agent</button><Link to="/chat" className={actionClass()}>Open Chat</Link></div></header>{started ? <div className="rounded-xl border border-border bg-brand-tint/20 px-4 py-3 text-[11px] text-brand-ink">Agent context prepared. Open Chat to continue.</div> : null}<div className="grid gap-4 md:grid-cols-2"><Panel className="p-5"><div className="flex items-center gap-2"><ShieldCheck className="size-4 text-primary"/><h2 className="text-[14px] font-semibold">Capabilities</h2></div><p className="mt-1.5 text-[11px] text-muted-foreground">Things this agent is designed to help with.</p><div className="mt-3"><CapabilityPills items={agent.jobs} limit={10}/></div></Panel><Panel className="p-5"><div className="flex items-center gap-2"><LockKeyhole className="size-4 text-primary"/><h2 className="text-[14px] font-semibold">Control</h2></div><ul className="mt-3 space-y-2 text-[11.5px] text-muted-foreground"><li className="flex gap-2"><CheckCircle2 className="mt-0.5 size-3.5 shrink-0 text-primary"/>Uses the same canonical Kurukoo services.</li><li className="flex gap-2"><CheckCircle2 className="mt-0.5 size-3.5 shrink-0 text-primary"/>Consequential actions can require your approval.</li><li className="flex gap-2"><CheckCircle2 className="mt-0.5 size-3.5 shrink-0 text-primary"/>Does not establish availability, price or completion by itself.</li></ul></Panel></div><Panel className="p-5"><h2 className="text-[14px] font-semibold">Good starting requests</h2><div className="mt-3 grid gap-2 sm:grid-cols-2">{agent.jobs.map((job) => <Link key={job} to="/chat" search={{ query: job } as never} className="rounded-xl border border-border bg-background px-3 py-2.5 text-[11px] font-medium hover:bg-elevated">{job}</Link>)}</div></Panel><Action onClick={start}>Prepare this agent for my next request</Action></div>; }
