import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, BriefcaseBusiness, Sparkles, Users, Zap } from "lucide-react";
export const Route = createFileRoute("/opportunities")({ component: Opportunities });
function Opportunities() { const items = [["Collaborate locally", "A community project is looking for someone with your skills.", Users], ["Turn a need into a service", "A recurring request could become a useful opportunity for a provider.", BriefcaseBusiness], ["Kurukoo proactive", "Kurukoo can surface relevant possibilities as context changes.", Zap]] as const; return <div className="w-full pb-12"><header className="mb-7 border-b border-border/70 pb-5"><p className="text-[11px] font-medium uppercase tracking-[0.14em] text-muted-foreground">Discover what could be next</p><h1 className="mt-1 text-[34px] font-semibold tracking-[-0.035em]">Opportunities</h1><p className="mt-2 max-w-2xl text-[14px] leading-relaxed text-muted-foreground">Useful possibilities surfaced from requests, people, places, conversations and proactive Kurukoo context.</p></header><div className="grid gap-3 md:grid-cols-3">{items.map(([title, body, Icon]) => <article key={title} className="rounded-2xl border border-border bg-surface p-5"><span className="grid size-9 place-items-center rounded-full bg-[#f4e6dc] text-[#765443]"><Icon className="size-4" /></span><h2 className="mt-4 text-[15px] font-semibold">{title}</h2><p className="mt-2 text-[12px] leading-relaxed text-muted-foreground">{body}</p><Link to="/chat" className="mt-4 inline-flex items-center gap-1 text-[11.5px] font-medium">Explore with Kurukoo <ArrowRight className="size-3.5" /></Link></article>)}</div><div className="mt-8 rounded-2xl bg-elevated/60 p-5"><div className="flex items-center gap-3"><Sparkles className="size-5 text-muted-foreground" /><div><p className="text-[13px] font-semibold">Have something in mind?</p><p className="mt-1 text-[11.5px] text-muted-foreground">Tell Kurukoo what you want to make happen and let the conversation find the next step.</p></div></div><Link to="/chat" className="mt-4 inline-flex rounded-full bg-foreground px-4 py-2 text-[11.5px] font-medium text-background">Start a conversation</Link></div></div>; }
