import { Link } from "@tanstack/react-router";
import {
  Bell, Briefcase, ChevronDown, ChevronLeft, ChevronRight, Compass, Ellipsis, ExternalLink, Home,
  ListChecks, MapPin, MessageSquare, Moon, Search, Sparkles, Sun, Tags, Users, Wallet, Zap,
} from "lucide-react";
import { useEffect, useState, type FormEvent, type ReactNode } from "react";
import { cn } from "@/lib/utils";
import { useKurukoo } from "@/lib/kurukoo-store";
import { fetchAuthenticatedAd, fetchPulseReadiness, type AuthenticatedAd, type PulseReadiness, activatePulse, deactivatePulse } from "@/lib/kurukoo-api";
import { ContextualTrustedRail } from "@/components/contextual-trusted-rail";
import { ReferralCard } from "@/components/kurukoo/referral-card";
import { logoutKurukoo } from "@/lib/kurukoo-auth";
const KURUKOO_LOGO_SRC = "https://raw.githubusercontent.com/temeaco-max/kurukoo/main/public/assets/brand/logo-icon.svg";
export function KurukooLogo({ className = "size-8" }: { className?: string }) { return <img src={KURUKOO_LOGO_SRC} alt="Kurukoo" width={32} height={38} className={cn("object-contain", className)} />; }
export function useProfileName() { const [name, setName] = useState("Ada"); useEffect(() => { const read = () => { const value = localStorage.getItem("kurukoo-profile-name")?.trim(); if (value) setName(value); }; read(); window.addEventListener("storage", read); window.addEventListener("kurukoo-profile-updated", read); return () => { window.removeEventListener("storage", read); window.removeEventListener("kurukoo-profile-updated", read); }; }, []); return name; }
export function PageHeader({ title, description, subtitle, eyebrow, action }: { title?: string; description?: string; subtitle?: string; eyebrow?: string; action?: ReactNode }) { return <header className="mb-7 flex items-start justify-between gap-4 border-b border-border/70 pb-5"><div className="min-w-0"><p className="text-[11px] font-bold uppercase tracking-[0.14em] text-primary">{eyebrow ?? "Kurukoo OS"}</p>{title ? <h1 className="mt-1.5 text-[28px] font-bold leading-[1.08] tracking-[-0.03em]">{title}</h1> : null}{(description ?? subtitle) ? <p className="mt-2 max-w-2xl text-[13px] leading-5 text-muted-foreground">{description ?? subtitle}</p> : null}</div>{action ? <div className="shrink-0">{action}</div> : null}</header>; }
export function EmptyState({ title, body, description }: { title: string; body?: string; description?: string }) { const message = body ?? description; return <div className="rounded-2xl border border-dashed border-border bg-elevated/30 px-5 py-8 text-center"><h2 className="text-[15px] font-semibold">{title}</h2>{message ? <p className="mx-auto mt-1.5 max-w-xl text-[13px] leading-relaxed text-muted-foreground">{message}</p> : null}</div>; }
const nav = [
  { to: "/workspace", label: "Your Perch", icon: Home }, { to: "/chat", label: "Chat / Voice", icon: MessageSquare },
  { to: "/explore", label: "Explore", icon: Compass }, { to: "/discover", label: "Nearby", icon: MapPin },
  { to: "/work", label: "Actions", icon: ListChecks }, { to: "/topics", label: "Topics", icon: Tags }, { to: "/activity", label: "Activity", icon: Bell },
] as const;
const more = [
  { to: "/network", label: "Network", icon: Users }, { to: "/capabilities", label: "Capabilities", icon: Sparkles },
  { to: "/providers", label: "Providers", icon: Users }, { to: "/businesses", label: "Businesses", icon: Briefcase },
  { to: "/creators", label: "Creators", icon: Sparkles }, { to: "/advertising", label: "Advertising", icon: Zap },
] as const;
function ThemeToggle() { const [dark, setDark] = useState(false); useEffect(() => { const stored = localStorage.getItem("kurukoo-theme"); const value = stored ? stored === "dark" : window.matchMedia("(prefers-color-scheme: dark)").matches; setDark(value); document.documentElement.classList.toggle("dark", value); }, []); return <button type="button" aria-label={dark ? "Use light appearance" : "Use dark appearance"} onClick={() => { const next = !dark; setDark(next); document.documentElement.classList.toggle("dark", next); localStorage.setItem("kurukoo-theme", next ? "dark" : "light"); }} className="grid size-9 place-items-center rounded-full text-muted-foreground hover:bg-elevated">{dark ? <Sun className="size-[18px]" /> : <Moon className="size-[18px]" />}</button>; }
function HeaderPresence() { const [readiness, setReadiness] = useState<PulseReadiness | null>(null); const [open, setOpen] = useState(false); const [skill, setSkill] = useState(""); const [busy, setBusy] = useState(false); const [message, setMessage] = useState(""); useEffect(() => { let cancelled = false; void fetchPulseReadiness().then((v) => { if (!cancelled) setReadiness(v); }).catch(() => { if (!cancelled) setReadiness(null); }); return () => { cancelled = true; }; }, []); const live = Boolean(readiness?.active); const eligible = Boolean(readiness?.eligibleToBroadcast); async function start() { if (!eligible) return; if (!skill.trim()) { setMessage("Tell Kurukoo what you are available for."); return; } if (!navigator.geolocation) { setMessage("Location is required before Go Live."); return; } setBusy(true); setMessage("Requesting your location…"); try { const p = await new Promise<GeolocationPosition>((resolve, reject) => navigator.geolocation.getCurrentPosition(resolve, reject, { enableHighAccuracy: false, maximumAge: 30000, timeout: 10000 })); await activatePulse(skill.trim(), p.coords.latitude, p.coords.longitude); setReadiness(await fetchPulseReadiness()); window.dispatchEvent(new Event("kurukoo-pulse-updated")); setMessage("You are live on Nearby Pulse."); } catch (error) { setMessage(error instanceof Error ? error.message : "Unable to start Nearby Pulse"); } finally { setBusy(false); } } async function stop() { setBusy(true); try { await deactivatePulse(); setReadiness(await fetchPulseReadiness()); window.dispatchEvent(new Event("kurukoo-pulse-updated")); setMessage("Nearby Pulse is off"); } catch (error) { setMessage(error instanceof Error ? error.message : "Unable to stop Nearby Pulse"); } finally { setBusy(false); } } return <div className="relative hidden sm:block"><button type="button" onClick={() => setOpen((v) => !v)} aria-expanded={open} aria-label={live ? "Live on Nearby Pulse" : "Go Live on Nearby Pulse"} className={cn("inline-flex items-center gap-1.5 rounded-full border px-2.5 py-1.5 text-[10.5px] font-medium", live ? "border-primary/25 bg-brand-tint text-brand-ink" : "border-border bg-surface text-muted-foreground hover:bg-elevated")}><span className={cn("size-1.5 rounded-full", live ? "bg-primary" : "bg-muted-foreground/40")} />{live ? "Live" : eligible ? "Go Live" : "Nearby"}</button>{open ? <div className="absolute right-0 top-[calc(100%+8px)] z-50 w-64 rounded-2xl border border-border bg-surface p-3 shadow-[var(--shadow-lift)]"><p className="text-[11px] font-semibold">{live ? "Your Pulse is live" : "Nearby Pulse"}</p><p className="mt-1 text-[10.5px] leading-4 text-muted-foreground">{live ? "Nearby people can discover your available service." : eligible ? "Choose what you are available for, then let nearby people discover you." : "Radar is available for local attention. Go Live requires provider readiness and verified availability."}</p>{live ? <button type="button" onClick={() => void stop()} disabled={busy} className="mt-3 min-h-8 w-full rounded-lg border border-border text-[10.5px] font-medium hover:bg-elevated disabled:opacity-50">{busy ? "Stopping…" : "Stop Go Live"}</button> : <><input value={skill} onChange={(e) => setSkill(e.target.value)} disabled={!eligible || busy} placeholder={eligible ? "e.g. oranges, suya, delivery" : "Provider skill"} className="mt-3 min-h-8 w-full rounded-lg border border-border bg-background px-2.5 py-2 text-[10.5px] outline-none disabled:opacity-50" /><button type="button" onClick={() => void start()} disabled={!eligible || busy} className="mt-1.5 min-h-8 w-full rounded-lg bg-primary text-[10.5px] font-medium text-primary-foreground disabled:opacity-50">{busy ? "Starting…" : "Go Live"}</button></>}</div> : null}</div>; }
function RailAd({ campaign }: { campaign: AuthenticatedAd | null }) { if (!campaign) return null; return <a href={campaign.clickUrl} rel="nofollow" className="mt-auto block overflow-hidden rounded-2xl border border-border bg-elevated/55 hover:bg-elevated"><div className="p-2.5"><div className="flex items-center justify-between gap-2"><p className="text-[9px] font-semibold uppercase tracking-[0.12em] text-muted-foreground">{campaign.disclosure}</p><ExternalLink className="size-3 text-muted-foreground" /></div>{campaign.image ? <img src={campaign.image} alt="" loading="lazy" width="176" height="70" className="mt-2 h-14 w-full rounded-xl object-cover" /> : null}<p className="mt-2 text-[11.5px] font-semibold leading-4">{campaign.title}</p><p className="mt-1 text-[10px] leading-4 text-muted-foreground">{campaign.desc}</p><span className="mt-2 inline-flex text-[10.5px] font-medium">{campaign.ctaText}</span></div></a>; }
function Header() { const name = useProfileName(); const { notifications } = useKurukoo(); const unread = notifications.filter((n) => !n.read).length; const [q, setQ] = useState(""); const [open, setOpen] = useState(false); const submit = (e: FormEvent<HTMLFormElement>) => { e.preventDefault(); const value = q.trim(); window.location.href = value ? `/explore?query=${encodeURIComponent(value)}` : "/explore"; }; const logout = async () => { setOpen(false); await logoutKurukoo(); window.location.href = "/"; }; return <header className="sticky top-0 z-50 border-b border-border/70 bg-background/95 backdrop-blur"><div className="relative flex h-[50px] items-center px-5 md:px-7 lg:px-8"><Link to="/" className="flex shrink-0 items-center gap-2" aria-label="Kurukoo home"><KurukooLogo /><span className="hidden text-[17px] font-semibold tracking-[-0.025em] sm:inline">Kurukoo</span></Link><div className="absolute left-1/2 hidden -translate-x-1/2 items-center gap-3 md:flex"><Link to="/" aria-label="Go to Kurukoo home" title="Kurukoo home" className="grid size-9 shrink-0 items-center justify-center rounded-full text-muted-foreground hover:bg-elevated"><Home className="size-[18px]" strokeWidth={1.8} /></Link><form onSubmit={submit} className="hidden w-[390px] md:block"><div className="flex h-10 items-center gap-2 rounded-xl border border-border bg-surface px-3"><Search className="size-4 text-muted-foreground" /><input aria-label="Search Kurukoo" value={q} onChange={(e) => setQ(e.target.value)} placeholder="Ask Kurukoo anything..." className="min-w-0 flex-1 bg-transparent text-[13px] outline-none" /><kbd className="hidden rounded-md border border-border bg-background px-1.5 py-0.5 text-[9px] text-muted-foreground lg:inline">⌘ K</kbd></div></form></div><div className="ml-auto flex shrink-0 items-center gap-1.5"><HeaderPresence /><Link to="/wallet" aria-label="Wallet" title="Wallet" className="hidden size-10 place-items-center rounded-full text-muted-foreground hover:bg-elevated sm:grid"><Wallet className="size-[18px]" /></Link><Link to="/activity" aria-label={unread ? `${unread} unread notifications` : "Notifications"} className="relative grid size-10 place-items-center rounded-full text-muted-foreground hover:bg-elevated"><Bell className="size-[19px]" />{unread ? <span className="absolute right-[8px] top-[7px] grid min-w-2.5 place-items-center rounded-full bg-destructive px-1 text-[7px] text-destructive-foreground">{unread > 9 ? "9+" : unread}</span> : null}</Link><span className="mx-1 h-8 w-px bg-border" aria-hidden /><div className="relative"><button type="button" onClick={() => setOpen((v) => !v)} aria-haspopup="menu" aria-expanded={open} className="flex min-w-0 items-center gap-2 rounded-xl px-1.5 py-1.5 hover:bg-elevated"><span className="grid size-9 shrink-0 place-items-center rounded-full bg-brand-tint text-[12px] font-semibold text-brand-ink">{name.charAt(0).toUpperCase() || "A"}</span><span className="hidden max-w-[110px] truncate text-[13px] font-medium sm:inline">{name}</span><ChevronDown className={cn("hidden size-4 text-muted-foreground transition-transform sm:block", open && "rotate-180")} /></button>{open ? <div role="menu" aria-label="Account menu" className="absolute right-0 top-[calc(100%+8px)] z-50 w-52 rounded-2xl border border-border bg-surface p-1.5 shadow-[var(--shadow-lift)]"><Link role="menuitem" to="/memory" onClick={() => setOpen(false)} className="block rounded-xl px-3 py-2.5 text-[12.5px] hover:bg-elevated">Memory</Link><Link role="menuitem" to="/settings" onClick={() => setOpen(false)} className="block rounded-xl px-3 py-2.5 text-[12.5px] hover:bg-elevated">Settings</Link><Link role="menuitem" to="/subscriptions" onClick={() => setOpen(false)} className="block rounded-xl px-3 py-2.5 text-[12.5px] hover:bg-elevated">Subscriptions</Link><Link role="menuitem" to="/advertising" onClick={() => setOpen(false)} className="block rounded-xl px-3 py-2.5 text-[12.5px] hover:bg-elevated">Advertise</Link><Link role="menuitem" to="/help" onClick={() => setOpen(false)} className="block rounded-xl px-3 py-2.5 text-[12.5px] hover:bg-elevated">Help</Link><div className="px-1"><ThemeToggle /></div><button type="button" role="menuitem" onClick={logout} className="mt-1 w-full rounded-xl px-3 py-2.5 text-left text-[12.5px] text-muted-foreground hover:bg-elevated">Log out</button></div> : null}</div></div></div><div className="px-5 pb-3 md:hidden"><form onSubmit={submit}><div className="flex h-10 items-center gap-2 rounded-xl border border-border bg-surface px-3"><Search className="size-4 text-muted-foreground" /><input aria-label="Search Kurukoo" value={q} onChange={(e) => setQ(e.target.value)} placeholder="Search Kurukoo" className="min-w-0 flex-1 bg-transparent text-[13px] outline-none" /></div></form></div></header>; }
export function AppShell({ children }: { children: ReactNode }) { const [collapsed, setCollapsed] = useState(false); const [trustedOpen, setTrustedOpen] = useState(true); const [moreOpen, setMoreOpen] = useState(false); const [ad, setAd] = useState<AuthenticatedAd | null>(null); useEffect(() => { if (!collapsed) void fetchAuthenticatedAd("left-rail").then(setAd).catch(() => setAd(null)); }, [collapsed]); const grid = trustedOpen ? collapsed ? "md:grid-cols-[76px_minmax(0,1fr)] lg:grid-cols-[76px_minmax(0,1fr)_224px]" : "md:grid-cols-[200px_minmax(0,1fr)] lg:grid-cols-[200px_minmax(0,1fr)_224px]" : collapsed ? "md:grid-cols-[76px_minmax(0,1fr)] lg:grid-cols-[76px_minmax(0,1fr)]" : "md:grid-cols-[200px_minmax(0,1fr)] lg:grid-cols-[200px_minmax(0,1fr)]"; return <div className="min-h-screen overflow-hidden bg-background"><Header /><div className={cn("h-[calc(100vh-50px)] min-h-0 overflow-hidden md:grid", grid)}><aside className={cn("relative z-30 min-h-0 h-full hidden flex-col border-r border-border bg-surface/90 py-0 backdrop-blur md:flex overflow-visible", collapsed ? "w-[76px] px-3" : "w-[200px] px-3")}><button type="button" onClick={() => setCollapsed((v) => !v)} aria-label={collapsed ? "Expand navigation" : "Collapse navigation"} title={collapsed ? "Expand navigation" : "Collapse navigation"} className="absolute right-[-18px] top-5 z-[80] grid size-9 place-items-center rounded-full border border-border bg-surface text-muted-foreground shadow-[var(--shadow-soft)] hover:bg-elevated">{collapsed ? <ChevronRight className="size-[17px]" strokeWidth={1.8} /> : <ChevronLeft className="size-[17px]" strokeWidth={1.8} />}</button><nav aria-label="OS navigation" className="scrollbar-none space-y-1 pt-5">{nav.map(({ to, label, icon: Icon }) => <Link key={to} to={to} activeProps={{ className: "bg-elevated font-medium text-foreground" }} className={cn("flex items-center rounded-xl py-2.5 text-[13.5px] leading-5 text-muted-foreground transition-colors hover:bg-elevated hover:text-foreground", collapsed ? "justify-center px-2" : "gap-3 px-3")} title={collapsed ? label : undefined}><Icon className="size-[18px] shrink-0" strokeWidth={1.8} />{!collapsed && <span>{label}</span>}</Link>)}</nav>{!collapsed ? <><div className="mt-3"><button type="button" onClick={() => setMoreOpen((v) => !v)} aria-expanded={moreOpen} aria-controls="kurukoo-more-navigation" className="flex w-full items-center justify-between rounded-xl px-3 py-2 text-[10px] font-medium uppercase tracking-[0.14em] text-muted-foreground hover:bg-elevated hover:text-foreground"><span>More</span><Ellipsis className="size-[17px]" strokeWidth={1.8} /></button>{moreOpen ? <nav id="kurukoo-more-navigation" aria-label="More navigation" className="scrollbar-none mt-1 max-h-[220px] space-y-1 overflow-y-auto overscroll-contain pr-1">{more.map(({ to, label, icon: Icon }) => <Link key={to} to={to} activeProps={{ className: "bg-elevated font-medium text-foreground" }} className="flex items-center gap-3 rounded-xl px-3 py-2.5 text-[13.5px] leading-5 text-muted-foreground transition-colors hover:bg-elevated hover:text-foreground"><Icon className="size-[18px] shrink-0" strokeWidth={1.8} /><span>{label}</span></Link>)}</nav> : null}</div><div className="mt-4"><ReferralCard /></div><RailAd campaign={ad} /></> : null}</aside><main className="min-h-0 h-full overflow-y-auto overscroll-contain"><div className="mx-auto min-w-0 max-w-[1600px] px-5 py-6 md:px-7 lg:px-8">{children}</div></main>{trustedOpen ? <ContextualTrustedRail open={trustedOpen} onOpenChange={setTrustedOpen} /> : <button type="button" onClick={() => setTrustedOpen(true)} aria-label="Open trusted context" title="Open trusted context" className="fixed right-2 top-1/2 z-30 grid size-9 -translate-y-1/2 place-items-center rounded-l-xl border border-border bg-surface text-muted-foreground shadow-[var(--shadow-soft)]"><ChevronLeft className="size-4" /></button>}</div></div>; }