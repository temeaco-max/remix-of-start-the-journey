import { Link } from "@tanstack/react-router";
import { Menu, X, Compass, MessageCircle, Sparkles, Wallet, Zap } from "lucide-react";
import { useState } from "react";
const items = [["/", "Home", Sparkles], ["/explore", "Explore", Compass], ["/topics", "Topics", MessageCircle], ["/opportunities", "Opportunities", Zap], ["/pricing", "Plans", Wallet]] as const;
function KurukooMark() { return <span aria-hidden className="grid size-7 shrink-0 place-items-center rounded-[10px] bg-[#f4e6dc] font-semibold text-[13px] leading-none text-[#765443]">K</span>; }
export function PublicMobileNavigation() { const [open, setOpen] = useState(false); return <><button type="button" onClick={() => setOpen(true)} aria-label="Open navigation" className="fixed left-4 top-3 z-50 grid size-9 place-items-center rounded-full border border-border bg-background/90 shadow-sm backdrop-blur md:hidden"><Menu className="size-[18px]" /></button>{open ? <><button type="button" aria-label="Close navigation" onClick={() => setOpen(false)} className="fixed inset-0 z-[60] bg-foreground/20 backdrop-blur-[2px] md:hidden" /><aside aria-label="Mobile public navigation" className="fixed inset-y-0 left-0 z-[70] flex w-[284px] flex-col border-r border-border bg-surface px-4 py-5 shadow-2xl md:hidden"><div className="flex items-center justify-between px-1 pb-7"><Link to="/" onClick={() => setOpen(false)} className="flex items-center gap-2"><KurukooMark /><span className="text-[17px] font-semibold tracking-[-0.025em]">Kurukoo</span></Link><button type="button" onClick={() => setOpen(false)} aria-label="Close navigation" className="grid size-9 place-items-center rounded-full hover:bg-elevated"><X className="size-[18px]" /></button></div><nav className="space-y-1">{items.map(([to,label,Icon], index) => { const className = `flex items-center gap-3 rounded-xl px-3 py-2.5 text-[13.5px] ${index === 0 ? "bg-[#f4e6dc] font-medium" : "text-muted-foreground hover:bg-elevated"}`; const content = <><Icon className="size-[18px]" />{label}</>; return to === "/opportunities" ? <a key={to} href={to} onClick={() => setOpen(false)} className={className}>{content}</a> : <Link key={to} to={to} onClick={() => setOpen(false)} className={className}>{content}</Link>; })}</nav><div className="mt-6 rounded-2xl bg-elevated/60 p-3"><p className="text-[11px] font-medium">Your workspace awaits.</p><p className="mt-1 text-[10.5px] leading-relaxed text-muted-foreground">Sign in to unlock your personal Kurukoo context.</p></div><div className="mt-auto"><Link to="/login" onClick={() => setOpen(false)} className="flex items-center justify-center rounded-xl border border-border px-3 py-2 text-[12px] font-medium">Log In</Link><Link to="/signup" onClick={() => setOpen(false)} className="mt-2 flex items-center justify-center rounded-xl bg-foreground px-3 py-2 text-[12px] font-medium text-background">Try for free</Link></div></aside></> : null}</>; }
