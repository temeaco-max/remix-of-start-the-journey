import { Link, useNavigate, useRouterState } from "@tanstack/react-router";
import { ChevronDown, Moon, Sun, ArrowRight, Compass, MessageCircle, MapPin, BriefcaseBusiness, Sparkles, Users, Store, PenLine, Handshake, Megaphone, Bot, BookOpen, Radio } from "lucide-react";
import { createPortal } from "react-dom";
import { useEffect, useRef, useState, type ReactNode } from "react";
import { AuthModal, type AuthMode } from "@/components/kurukoo/auth";
import { PublicRail, RotatingPublicPrompt } from "@/components/public-kurukoo-rail";
import { PublicContextRail } from "@/components/public-kurukoo-context";
import { PublicMobileNavigation } from "@/components/public-kurukoo-mobile-nav";
export { PublicHome } from "@/components/public-kurukoo-home";

const meetKurukoo = [
  { heading: "Get things done", items: [["/chat", "Ask Kurukoo", "Start with a request in your own words.", MessageCircle], ["/explore", "Explore", "Browse ways to get everyday jobs done.", Compass], ["/discover", "Nearby", "See useful places, people and opportunities around you.", MapPin], ["/work", "Work", "Keep active requests and outcomes moving.", BriefcaseBusiness]] },
  { heading: "Your Kurukoo network", items: [["/agents", "Agents", "Specialist AI help for different kinds of work.", Bot], ["/connect", "Connect", "Bring supported services and sources into Kurukoo.", Radio], ["/topics", "Community", "Useful local knowledge, discussion and context.", Users], ["/opportunities", "Opportunities", "Find ways to work, collaborate or take action.", Handshake]] },
  { heading: "Learn & discover", items: [["/how-it-works", "How it works", "See how a request becomes a real outcome.", Sparkles], ["/resources", "Resources", "Short guides and visual help for using Kurukoo.", BookOpen], ["/capabilities", "What can I get done?", "Browse the jobs Kurukoo can help with.", ArrowRight], ["/pricing", "Plans", "See how Kurukoo can fit the way you work.", Store]] },
  { heading: "For the network", items: [["/providers", "Providers", "Turn useful services into actionable options.", Store], ["/businesses", "Businesses", "Receive, manage and respond to real requests.", BriefcaseBusiness], ["/creators", "Creators", "Turn trusted recommendations into action.", PenLine], ["/advertising", "Advertising", "Reach people through relevant, useful moments.", Megaphone]] },
] as const;
const forYou = [
  ["/people", "People", "Get everyday life moving with Kurukoo.", Users], ["/providers", "Providers", "Turn demand into qualified, actionable requests.", Store], ["/businesses", "Businesses", "Coordinate customers, work and opportunities.", BriefcaseBusiness], ["/creators", "Creators", "Help people discover things worth acting on.", PenLine], ["/contributors", "Contributors", "Share knowledge, resources and useful context.", Handshake], ["/partners", "Partners", "Build services and distribution around Kurukoo.", Handshake], ["/advertising", "Advertisers", "Reach people with relevant commercial opportunities.", Megaphone], ["/agents", "AI agents", "Build and use specialist help for real work.", Bot],
] as const;
const routerDestinations = new Set(["/", "/explore", "/discover", "/how-it-works", "/capabilities", "/topics", "/about", "/blog", "/help", "/login", "/signup", "/contributors", "/partners", "/pricing", "/contact", "/use-cases", "/opportunities", "/advertising", "/providers", "/businesses", "/creators", "/people", "/agents", "/connect", "/resources", "/legal", "/cookies", "/chat", "/work"]);
const canonicalLogo = "https://raw.githubusercontent.com/temeaco-max/kurukoo/ca35abf26b6b07fa234f6b986bd7a52e4e718f86/public/assets/brand/logo-icon.png";
function KurukooMark({ className = "size-6" }: { className?: string }) { return <img src={canonicalLogo} alt="Kurukoo" aria-hidden="true" className={`shrink-0 object-contain ${className}`} width="32" height="32" />; }
function PublicDestinationLink({ to, children, className, onClick }: { to: string; children: ReactNode; className: string; onClick?: () => void }) { return routerDestinations.has(to) ? <Link to={to as never} onClick={onClick} className={className}>{children}</Link> : <a href={to} onClick={onClick} className={className}>{children}</a>; }
function PublicMenu({ label, kind, items, railCollapsed }: { label: string; kind: "meet" | "you"; items: readonly unknown[]; railCollapsed: boolean }) {
  const [open, setOpen] = useState(false); const ref = useRef<HTMLDivElement>(null); const menuId = `public-menu-${kind}`;
  useEffect(() => { if (!open) return; const close = (event: MouseEvent) => { if (!ref.current?.contains(event.target as Node)) setOpen(false); }; const escape = (event: KeyboardEvent) => { if (event.key === "Escape") { setOpen(false); (ref.current?.querySelector("button") as HTMLButtonElement | null)?.focus(); } }; const closeOnRoute = () => setOpen(false); document.addEventListener("mousedown", close); document.addEventListener("keydown", escape); window.addEventListener("popstate", closeOnRoute); return () => { document.removeEventListener("mousedown", close); document.removeEventListener("keydown", escape); window.removeEventListener("popstate", closeOnRoute); }; }, [open]);
  useEffect(() => { const close = () => setOpen(false); window.addEventListener("resize", close); return () => window.removeEventListener("resize", close); }, []);
  const menu = open && typeof document !== "undefined" ? createPortal(<><button type="button" aria-label="Close menu" onClick={() => setOpen(false)} className="fixed inset-x-0 bottom-0 top-[50px] z-[60] bg-foreground/5" /><div role="menu" id={menuId} className="fixed top-[50px] z-[70] max-h-[calc(100vh-50px)] overflow-y-auto border-b border-border/80 bg-surface/98 shadow-[0_18px_45px_rgba(0,0,0,0.10)] backdrop-blur-xl" style={{ left: railCollapsed ? 76 : 200, right: 224 }}>
      <div className="mx-auto w-full max-w-[1120px] px-5 py-6 md:px-8">
        {kind === "meet" ? <div className="grid grid-cols-1 gap-7 sm:grid-cols-2 lg:grid-cols-4">{(items as typeof meetKurukoo).map((column) => <div key={column.heading} className="min-w-0"><p className="mb-3 px-2 text-[10px] font-semibold uppercase tracking-[0.12em] text-muted-foreground">{column.heading}</p><div className="space-y-1">{column.items.map(([to, title, description, Icon]) => <PublicDestinationLink key={to} to={to} onClick={() => setOpen(false)} className="group block p-2.5 hover:bg-elevated"><span className="flex items-start gap-3"><span className="mt-0.5 grid size-8 shrink-0 place-items-center bg-elevated text-muted-foreground group-hover:text-foreground"><Icon className="size-4" strokeWidth={1.8} /></span><span className="min-w-0"><span className="block text-[12px] font-semibold text-foreground">{title}</span><span className="mt-0.5 block text-[10.5px] leading-4 text-muted-foreground">{description}</span></span></span></PublicDestinationLink>)}</div></div>)}</div> : <div className="grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-4">{(items as typeof forYou).map(([to, title, description, Icon]) => <PublicDestinationLink key={to} to={to} onClick={() => setOpen(false)} className="group border border-border/70 bg-background/50 p-4 hover:border-border hover:bg-elevated"><span className="flex items-start gap-3"><span className="grid size-9 shrink-0 place-items-center bg-elevated text-muted-foreground group-hover:text-foreground"><Icon className="size-4" strokeWidth={1.8} /></span><span className="min-w-0"><span className="block text-[12px] font-semibold text-foreground">{title}</span><span className="mt-1 block text-[10.5px] leading-4 text-muted-foreground">{description}</span></span></span></PublicDestinationLink>)}</div>}
        <div className="mt-6 flex items-center justify-between border-t border-border/60 pt-4"><p className="text-[10.5px] text-muted-foreground">{kind === "meet" ? "One place to discover, coordinate and get things moving." : "Kurukoo adapts to the people and organisations using it."}</p><PublicDestinationLink to={kind === "meet" ? "/how-it-works" : "/use-cases"} onClick={() => setOpen(false)} className="inline-flex items-center gap-1 text-[10.5px] font-medium text-foreground hover:opacity-70">{kind === "meet" ? "See how Kurukoo works" : "See what Kurukoo can do"}<ArrowRight className="size-3" /></PublicDestinationLink></div>
      </div>
    </div></>, document.body) : null;
  return <div ref={ref} className="relative h-full">
    <button type="button" onClick={() => setOpen((v) => !v)} aria-expanded={open} aria-controls={menuId} className="inline-flex h-full items-center gap-1 transition-colors hover:text-foreground">{label}<ChevronDown className={`size-3.5 text-muted-foreground transition-transform dark:text-primary ${open ? "rotate-180" : ""}`} /></button>
    {menu}
  </div>;
}
function PublicThemeToggle() {
  const [dark, setDark] = useState(false);
  useEffect(() => { const stored = localStorage.getItem("kurukoo-theme"); const value = stored ? stored === "dark" : window.matchMedia("(prefers-color-scheme: dark)").matches; setDark(value); document.documentElement.classList.toggle("dark", value); }, []);
  return <button type="button" onClick={() => { const next = !dark; setDark(next); document.documentElement.classList.toggle("dark", next); localStorage.setItem("kurukoo-theme", next ? "dark" : "light"); }} aria-label={dark ? "Use day mode" : "Use night mode"} title={dark ? "Switch to day mode" : "Switch to night mode"} className="grid size-9 place-items-center rounded-full text-muted-foreground hover:bg-elevated hover:text-foreground">{dark ? <Sun className="size-[18px]" /> : <Moon className="size-[18px]" />}</button>;
}
export function PublicKurukooShell({ children }: { children: ReactNode }) {
  const pathname = useRouterState({ select: (s) => s.location.pathname }); const navigate = useNavigate(); const routeMode: AuthMode | null = pathname === "/login" ? "login" : pathname === "/signup" ? "signup" : null; const [authMode, setAuthMode] = useState<AuthMode | null>(routeMode); const [signedIn, setSignedIn] = useState(false); const [railCollapsed, setRailCollapsed] = useState(false);
  useEffect(() => { setAuthMode(routeMode); }, [routeMode]); useEffect(() => { const read = () => setSignedIn(localStorage.getItem("kurukoo-authenticated") === "true"); read(); window.addEventListener("kurukoo-auth-updated", read); return () => window.removeEventListener("kurukoo-auth-updated", read); }, []); const openAuth = (mode: AuthMode) => setAuthMode(mode); const closeAuth = () => { setAuthMode(null); if (routeMode) navigate({ to: "/" }); };
  return <div className="min-h-screen overflow-hidden bg-background"><header className="sticky top-0 z-50 border-b border-border/70 bg-background/95 backdrop-blur"><div className="relative flex h-[50px] items-center px-5 md:px-7 lg:px-8"><Link to="/" className="flex shrink-0 items-center gap-2" aria-label="Kurukoo home"><KurukooMark className="size-8" /><span className="text-[17px] font-semibold tracking-[-0.025em]">Kurukoo</span></Link><nav aria-label="Kurukoo" className="absolute left-1/2 hidden h-full -translate-x-1/2 items-center justify-center gap-7 text-[12.5px] text-muted-foreground md:flex"><PublicMenu label="Meet Kurukoo" kind="meet" items={meetKurukoo} railCollapsed={railCollapsed} /><PublicDestinationLink to="/how-it-works" className="transition-colors hover:text-foreground">How it works</PublicDestinationLink><PublicDestinationLink to="/explore" className="transition-colors hover:text-foreground">Explore</PublicDestinationLink><PublicMenu label="For You" kind="you" items={forYou} railCollapsed={railCollapsed} /><PublicDestinationLink to="/topics" className="transition-colors hover:text-foreground">Community</PublicDestinationLink></nav><div className="ml-auto flex items-center gap-1.5"><RotatingPublicPrompt /><PublicThemeToggle /><button type="button" onClick={() => openAuth("login")} className="rounded-full px-3 py-1.5 text-[12px] font-medium text-muted-foreground hover:bg-elevated hover:text-foreground">Log In</button><button type="button" onClick={() => openAuth("signup")} className="rounded-full bg-primary px-3.5 py-1.5 text-[12px] font-medium text-primary-foreground">Try for free</button></div></div></header><div className={`h-[calc(100vh-50px)] min-h-0 overflow-hidden md:grid ${railCollapsed ? "md:grid-cols-[76px_minmax(0,1fr)]" : "md:grid-cols-[200px_minmax(0,1fr)]"}`}><PublicRail collapsed={railCollapsed} onToggle={() => setRailCollapsed((v) => !v)} onAuth={openAuth} /><div className="min-h-0 h-full overflow-y-auto scrollbar-none"><div className="grid min-h-full lg:grid-cols-[minmax(0,1fr)_224px]"><main className="flex min-w-0 min-h-full flex-col"><div className="mx-auto w-full max-w-[1120px] flex-1 px-5 py-9 md:px-8 md:py-12">{children}</div><footer className="sticky bottom-0 z-20 mx-auto flex w-full max-w-[1120px] items-center justify-between gap-4 border-t border-border/60 bg-background/95 px-5 py-5 text-[11px] text-muted-foreground backdrop-blur md:px-8"><p className="flex shrink-0 items-center gap-2"><img src={canonicalLogo} alt="Kurukoo" className="size-4 object-contain" width="16" height="16" /><span>© 2026 Kurukoo OS · Everyday AI OS for real life.</span></p><div className="ml-auto flex flex-wrap items-center justify-end gap-x-4 gap-y-2"><span className="text-[10px] font-medium uppercase tracking-[0.1em] text-muted-foreground/80">Prototype build</span><nav aria-label="Information and legal"><div className="flex flex-wrap justify-end gap-x-4 gap-y-2"><PublicDestinationLink to="/about" className="hover:text-foreground">About</PublicDestinationLink><PublicDestinationLink to="/help" className="hover:text-foreground">Help</PublicDestinationLink><PublicDestinationLink to="/legal" className="font-medium text-foreground hover:opacity-80">Legal & policies</PublicDestinationLink><PublicDestinationLink to="/legal/privacy" className="hover:text-foreground">Privacy</PublicDestinationLink><PublicDestinationLink to="/legal/terms" className="hover:text-foreground">Terms</PublicDestinationLink><PublicDestinationLink to="/legal/cookies" className="hover:text-foreground">Cookies</PublicDestinationLink></div></nav></div></footer></main><aside className="hidden min-w-0 border-l border-border/45 lg:block"><PublicContextRail signedIn={signedIn} onOpenAuth={openAuth} /></aside></div></div></div><PublicMobileNavigation onAuth={openAuth} />{authMode ? <AuthModal mode={authMode} onClose={closeAuth} onModeChange={setAuthMode} /> : null}</div>;
}
