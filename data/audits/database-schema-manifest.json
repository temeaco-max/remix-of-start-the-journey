{
  "schema_version": "physical-execution-profile-v1",
  "schema_path": "src/database.ts",
  "schema_git_blob_sha": "a1bf2166f231804e1145e530c04fc45e335d2a2c",
  "migration_policy": "Future schema changes require a reviewed migration/update and an explicit manifest refresh; runtime CREATE TABLE IF NOT EXISTS remains the legacy bootstrap baseline until the persistence adapter is cut over.",
  "migration_note": "Adds skills.execution_profile_json for the bounded physical-execution capability profile. Existing databases receive the column through the canonical backwards-compatible schema guard; no delivery-specific table or lifecycle was created."
}
